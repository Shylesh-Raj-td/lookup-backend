name: Backend CI/CD

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to Docker Hub
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: Build Docker image
        run: |
          docker build -t shyleshraj/lookup-backend:latest .

      - name: Push Docker image
        run: |
          docker push shyleshraj/lookup-backend:latest

      - name: Deploy to Heroku
        run: |
          echo "${{ secrets.HEROKU_API_KEY }}" | heroku container:login
          docker tag shyleshraj/lookup-backend:latest registry.heroku.com/your-backend-app-name/web
          docker push registry.heroku.com/your-backend-app-name/web
          heroku container:release web -a your-backend-app-name
